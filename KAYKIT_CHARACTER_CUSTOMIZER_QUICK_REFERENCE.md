# KayKit Character Customizer - Quick Reference\n\n## 📋 Component APIs\n\n### CharacterCustomizer Component\n```tsx\n<CharacterCustomizer\n  characterPath=\"/Assets/KayKit_Adventurers_2.0_FREE/.../Rogue.glb\"\n  characterName=\"Rogue\"\n  onSave={(variantId, name) => {}}\n  onCancel={() => {}}\n  canvasWidth={800}\n  canvasHeight={600}\n/>\n```\n\n### useCustomizableCharacter Hook\n```tsx\nconst {\n  model,                                    // THREE.Group | null\n  materials,                               // THREE.Material[]\n  colorPalette,                            // ExtractedColor[]\n  selectedColorIndex,                      // number | null\n  colorSwaps,                              // ColorSwap[]\n  selectColor,                             // (index: number) => void\n  replaceColor,                            // (orig: RGB, new: RGB, tol?: number) => Promise<void>\n  undoLastChange,                          // () => void\n  loadVariant,                             // (id: string) => Promise<void>\n  saveVariant,                             // (name: string, tags?: string[]) => Promise<string>\n  isLoading,                               // boolean\n  error,                                   // string | null\n} = useCustomizableCharacter({ characterPath })\n```\n\n## 🧩 Core Modules\n\n### TextureExtractor\n```tsx\n// Extract unique colors\nconst result = await TextureExtractor.extractColors(\n  texture,\n  'smart',  // 'smart' | 'grid' | 'all'\n  15,       // clusterThreshold\n  5         // minPixelThreshold\n);\n\n// result contains:\n// - colors: ExtractedColor[]\n// - canvasData: ImageData\n// - textureWidth: number\n// - textureHeight: number\n```\n\n### ColorReplacement\n```tsx\n// Single replacement\nColorReplacement.replaceColor(canvas, [255,100,50], [200,50,200], { tolerance: 20 });\n\n// Batch replacement\nColorReplacement.replaceMultipleColors(canvas, [\n  { original: [255,100,50], replacement: [200,50,200] },\n  { original: [100,100,100], replacement: [50,200,100] },\n]);\n\n// Update material\nColorReplacement.updateMaterialTexture(material, canvas);\n\n// Update all materials in group\nColorReplacement.updateGroupMaterials(group, canvas);\n```\n\n### ModelCloner\n```tsx\n// Clone model preserving rigging\nconst cloned = ModelCloner.cloneGltfModel(gltf);\n\n// Access properties\ncloned.scene;           // THREE.Group\ncloned.materials;       // Material[]\ncloned.skeleton;        // Skeleton | null\ncloned.metadata;        // { textureAtlas, boneCount, meshCount }\n\n// Validate bones\nconst valid = ModelCloner.validateBoneStructure(cloned.scene);\n\n// Get animations\nconst anims = ModelCloner.getAnimations(gltf);\n\n// Cleanup\nModelCloner.disposeClonedModel(cloned);\n```\n\n### CharacterPresets\n```tsx\n// Save\nconst id = await CharacterPresets.saveVariant({\n  name: \"Variant Name\",\n  baseModel: \"rogue\",\n  baseModelPath: \"/path/to/model.glb\",\n  textureDataUrl: \"data:image/png;base64,...\",\n  colorSwaps: [],\n  metadata: { version: \"1.0\", ... }\n});\n\n// Load\nconst variant = await CharacterPresets.loadVariant(id);\n\n// Get all\nconst variants = await CharacterPresets.getAllVariants();\nconst rogueVariants = await CharacterPresets.getVariantsByModel('rogue');\n\n// Update\nawait CharacterPresets.updateVariant(id, { name: \"New Name\" });\n\n// Delete\nawait CharacterPresets.deleteVariant(id);\n\n// Storage info\nconst { usage, quota } = await CharacterPresets.getStorageInfo();\n```\n\n## 🎯 Type Definitions\n\n```typescript\n// Color utilities\ntype RGB = [number, number, number];\ntype RGBA = [number, number, number, number];\n\ninterface ExtractedColor {\n  hex: string;           // \"#FF6432\"\n  rgb: RGB;\n  rgba: RGBA;\n  pixelCount: number;\n  indexInPalette: number;\n  percentage: number;    // 0-100\n}\n\ninterface ColorSwap {\n  original: RGB;\n  replacement: RGB;\n  tolerance?: number;    // 0-255\n  label?: string;        // \"Cloak\", \"Boots\", etc.\n}\n\ninterface SavedCharacterVariant {\n  id: string;\n  name: string;\n  baseModel: 'rogue' | 'knight' | 'ranger' | 'mage' | 'barbarian';\n  baseModelPath: string;\n  textureDataUrl: string;  // Base64 PNG\n  colorSwaps: ColorSwap[];\n  metadata: {\n    version: string;\n    createdAt: string;      // ISO8601\n    modifiedAt: string;     // ISO8601\n    author?: string;\n    description?: string;\n    tags: string[];\n  };\n}\n```\n\n## 🔧 Common Patterns\n\n### Full Customization Workflow\n```tsx\nimport { useCustomizableCharacter } from '@/hooks/useCustomizableCharacter';\nimport CharacterCustomizer from '@/components/CharacterCustomizer';\n\nfunction MyCharacterCustomizer() {\n  const [showCustomizer, setShowCustomizer] = useState(false);\n  \n  const {\n    colorPalette,\n    selectedColorIndex,\n    selectColor,\n    replaceColor,\n    saveVariant,\n  } = useCustomizableCharacter({\n    characterPath: '/path/to/Rogue.glb',\n  });\n\n  const handleColorClick = (index: number) => {\n    selectColor(index);\n    // Show color picker\n  };\n\n  const handleColorChange = async (newColor: RGB) => {\n    const selectedColor = colorPalette[selectedColorIndex];\n    await replaceColor(selectedColor.rgb, newColor);\n  };\n\n  const handleSave = async (name: string) => {\n    const variantId = await saveVariant(name);\n    console.log('Saved variant:', variantId);\n  };\n\n  return (\n    <div>\n      {showCustomizer ? (\n        <CharacterCustomizer\n          characterPath=\"/path/to/Rogue.glb\"\n          characterName=\"Rogue\"\n          onSave={(variantId, name) => {\n            handleSave(name);\n            setShowCustomizer(false);\n          }}\n          onCancel={() => setShowCustomizer(false)}\n        />\n      ) : (\n        <button onClick={() => setShowCustomizer(true)}>\n          Customize Rogue\n        </button>\n      )}\n    </div>\n  );\n}\n```\n\n### Loading a Saved Variant\n```tsx\nimport { CharacterPresets } from '@/modules/character-customizer/CharacterPresets';\nimport { useCustomizableCharacter } from '@/hooks/useCustomizableCharacter';\n\nfunction LoadVariantExample() {\n  const { loadVariant } = useCustomizableCharacter({\n    characterPath: '/path/to/Rogue.glb',\n  });\n\n  const handleLoadVariant = async (variantId: string) => {\n    try {\n      await loadVariant(variantId);\n      console.log('Variant loaded!');\n    } catch (error) {\n      console.error('Failed to load:', error);\n    }\n  };\n\n  return (\n    <button onClick={() => handleLoadVariant('variant-12345-abc')}>\n      Load Variant\n    </button>\n  );\n}\n```\n\n### Batch Color Operations\n```tsx\nimport { ColorReplacement } from '@/modules/character-customizer/ColorReplacement';\n\n// Darken all colors\nfunction darkenAllColors(canvas: HTMLCanvasElement, colors: ExtractedColor[]) {\n  const swaps = colors.map(c => ({\n    original: c.rgb,\n    replacement: ColorReplacement.darkenColor(c.rgb, 50),\n  }));\n  return ColorReplacement.replaceMultipleColors(canvas, swaps);\n}\n\n// Randomize\nfunction randomizeColors(canvas: HTMLCanvasElement, colors: ExtractedColor[]) {\n  const swaps = colors.map(c => ({\n    original: c.rgb,\n    replacement: [\n      Math.floor(Math.random() * 256),\n      Math.floor(Math.random() * 256),\n      Math.floor(Math.random() * 256),\n    ] as RGB,\n  }));\n  return ColorReplacement.replaceMultipleColors(canvas, swaps);\n}\n```\n\n## ⚙️ Configuration\n\n### Color Extraction Settings\n```tsx\n// Smart extraction (recommended)\nawait TextureExtractor.extractColors(\n  texture,\n  'smart',    // Focus on opaque pixels\n  15,         // Match within 15 RGB units\n  5           // Ignore colors with < 5 pixels\n);\n\n// Grid extraction (faster)\nawait TextureExtractor.extractColors(\n  texture,\n  'grid',     // Sample in grid pattern\n  15,\n  5\n);\n\n// All pixels (most accurate, slower)\nawait TextureExtractor.extractColors(\n  texture,\n  'all',      // Analyze every pixel\n  15,\n  5\n);\n```\n\n### Color Replacement Tolerance\n```tsx\n// Strict matching (only exact colors)\nColorReplacement.replaceColor(canvas, original, replacement, { tolerance: 0 });\n\n// Moderate matching (recommended)\nColorReplacement.replaceColor(canvas, original, replacement, { tolerance: 20 });\n\n// Fuzzy matching (similar colors)\nColorReplacement.replaceColor(canvas, original, replacement, { tolerance: 50 });\n```\n\n## 📊 Storage\n\n### Browser Storage Limits\n- **IndexedDB**: ~50MB per domain (can request more)\n- **LocalStorage**: ~5MB per domain\n- **Texture PNG**: ~2-5MB per variant\n- **Variant JSON**: ~1-5KB per variant\n\n### Check Storage\n```tsx\nconst { usage, quota } = await CharacterPresets.getStorageInfo();\nconst percentUsed = (usage / quota) * 100;\nconsole.log(`Using ${percentUsed.toFixed(1)}% of available storage`);\n```\n\n### Clear Storage\n```tsx\nawait CharacterPresets.clearAll();\n```\n\n## 🎨 Color Utilities\n\n```tsx\n// RGB to Hex\nfunction rgbToHex(rgb: RGB): string {\n  return `#${[...rgb].map(x => x.toString(16).padStart(2, '0')).join('')}`;\n}\n\n// Hex to RGB\nfunction hexToRgb(hex: string): RGB {\n  const result = /^#?([a-f\\d]{2})([a-f\\d]{2})([a-f\\d]{2})$/i.exec(hex);\n  return [parseInt(result[1], 16), parseInt(result[2], 16), parseInt(result[3], 16)];\n}\n\n// Lighten\nfunction lighten(rgb: RGB, amount: number): RGB {\n  return [Math.min(255, rgb[0] + amount), Math.min(255, rgb[1] + amount), Math.min(255, rgb[2] + amount)];\n}\n\n// Darken\nfunction darken(rgb: RGB, amount: number): RGB {\n  return [Math.max(0, rgb[0] - amount), Math.max(0, rgb[1] - amount), Math.max(0, rgb[2] - amount)];\n}\n```\n\n## 🐛 Debug Logging\n\n```tsx\n// Enable debug logs\nimport { TextureExtractor } from '@/modules/character-customizer/TextureExtractor';\n\nconst result = await TextureExtractor.extractColors(texture, 'smart');\nconsole.log('Colors found:', result.uniqueColorCount);\nconsole.log('Unique colors:', result.colors.map(c => c.hex));\nconsole.log('Color distribution:', result.colors.map(c => ({ hex: c.hex, pct: c.percentage })));\n```\n\n## 📚 Related Documentation\n\n- [KAYKIT_CHARACTER_CUSTOMIZER_PLAN.md](KAYKIT_CHARACTER_CUSTOMIZER_PLAN.md) - Full implementation plan\n- [KAYKIT_CHARACTER_CUSTOMIZER_INTEGRATION.md](KAYKIT_CHARACTER_CUSTOMIZER_INTEGRATION.md) - Detailed integration guide\n- [ASSET_AND_ANIMATION_INDEX.md](ASSET_AND_ANIMATION_INDEX.md) - Asset pipeline documentation\n- [THREEJS_GAME_DEVELOPMENT_GUIDE.md](THREEJS_GAME_DEVELOPMENT_GUIDE.md) - Three.js best practices\n